kind = "web"
previewPath = "/"
title = "AG Code Agent"
version = "1.0.0"
id = "artifacts/ag-code-agent"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24669

[services.development]
run = "pnpm --filter @workspace/ag-code-agent run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ag-code-agent", "run", "build" ]
publicDir = "artifacts/ag-code-agent/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24669"
BASE_PATH = "/"
